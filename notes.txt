docker pull osrf/ros:noetic-desktop-full
docker run -it --net=host osrf/ros:noetic-desktop-full roscore
docker run -it --net=host --mount type=bind,source=/home/hsken/Documents/xhand1,destination=/mnt/xhand1 osrf/ros:noetic-desktop-full /bin/bash

sudo apt-get update
sudo apt-get install libcrypto++-dev libcrypto++-doc libcrypto++-utils 

# Build controllers
sudo apt-get update && sudo apt-get install python3-catkin-tools python3-osrf-pycommon
cd /mnt/xhand1/dexterous_hands/src/ && catkin build xhand_control_ros xhand_controller xhand_socket xhand_utils && source ../devel/setup.bash


# Controller
source /mnt/xhand1/dexterous_hands/devel/setup.bash
roslaunch xhand_control_ros xhand_control.launch

# GUI-enabled
docker run -it --net=host --mount type=bind,source=/home/hsken/Documents/xhand1,destination=/mnt/xhand1 --env="DISPLAY" --env="QT_X11_NO_MITSHM=1" --volume="/tmp/.X11-unix:/tmp/.X11-unix:rw" --device=/dev/dri:/dev/dri osrf/ros:noetic-desktop-full /bin/bash
mkdir -p -m 0700 /run/user/0 && export XDG_RUNTIME_DIR=/run/user/0
sudo apt-get update && sudo apt-get install pip python3-tk && pip install matplotlib pandas nptyping

(outside container)
sudo xhost +local:`sudo docker inspect --format='{{ .Config.Hostname }}' <docker id>`



python scripts/deploy_vla.py --checkpoint models/version_1/ --device cuda --task "Wait for the grasp position, then grab and hold the box" --chunk-size 15
